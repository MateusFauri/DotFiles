{
	"layer": "top",
	//"modules-left": ["custom/launcher","cpu","memory","tray","custo/media"],
	//"modules-center": ["river/tags", "sway/workspaces"],
	//"modules-right": ["custom/updates","clock","pulseaudio", "battery","network","custom/power"],
	
	"modules-left": ["custom/launcher","sway/workspaces","tray","custo/media"],
	"modules-center": ["clock"],
	"modules-right": ["custom/updates","cpu","memory", "pulseaudio", "battery","network","custom/power"],
    
    "pulseaudio": {
        "tooltip-format": "{icon} {desc} // {volume}%",
        "format-muted":"",
	    "scroll-step": 5,
	    "format": "{icon} {volume}%",
	    "format-muted": "{icon} {volume}%",
	    "format-bluetooth": " {icon} {volume}% ",
        "format-bluetooth-muted": " {icon} {volume}%",
        "on-click": "pavucontrol -t 3",
	    "format-icons": {
            "default": ["", "", "󰕾", "󰕾", "󰕾", " ", " ", " "]
		}
	},

    "river/tags": {
		"num-tags": 6
    },

    "sway/workspaces": {
		"disable-scroll": true,
		"format": "{name}",
		"all-outputs": true
	},

     "network": {
        //"interface": "wlp2s0", // (Optional) To force the use of this interface
        "format-wifi": "   ({signalStrength}%)",
        "format-ethernet": "{ifname}: {ipaddr}/{cidr} 󰈁",
        "format-disconnected": "Disconnected ⚠",
        "on-click-right": "kitty --start-as normal bash -ci nmcli",
        //"on-click": "kitty --start-as normal bash -ci what",
        "format-alt": "{ifname}: {ipaddr}/{cidr}"
    },

	"backlight": {
		"tooltip": false,
		"format": " {}%",
		"interval":1,
        "on-scroll-up": "light -A 5",
		"on-scroll-down": "light -U 5"
	},
    "battery": {
        "states": {
            "good": 95,
            "warning": 30,
            "critical": 20
        },
        "format": "{icon}  {capacity}%",
        "format-charging": " {capacity}%",
        "format-plugged": " {capacity}%",
        "format-alt": "{time} {icon}",
        "format-icons": ["", "", "", "", ""]
    },
	"tray":{
		"icon-size":18,
		"spacing": 10
	},

    //"clock": {
    //   "format": "{: %I:%M %p %d/%m/%Y}"
    //},

    "clock": {
        "timezone": "America/São-Paulo",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
        "format": "{: %I:%M %p - %d %b %A}"
    },

    "cpu": {
        "interval": 15,
        "format": " {}%",
        "max-length": 10
    },
    "memory": {
        "interval": 30,
        "format": " {}%",
        "max-length": 10
    },
    "custom/media": {
        "interval": 30,
        "format": "{icon} {}",
        "return-type": "json",
        "max-length": 20,
        "format-icons": {
            "spotify": " ",
            "default": "󰎆 "
        },
        "escape": true,
        "exec": "$HOME/.config/system_scripts/mediaplayer.py 2> /dev/null",
        "on-click": "playerctl play-pause"
    },
    "custom/launcher":{
        "format":" ",
        "on-click": "rofi -show drun",
        "on-click-right": "killall rofi"
    },
    "custom/power":{
        "format": " ",
        "on-click": "bash ~/.config/rofi/leave/leave.sh",
    },
    "custom/updates":{
        "format": "{} Update(s)",
        "exec": "checkupdates | wc -l",
        "exec-if": "[[ $(checkupdates | wc -l) != 0 ]]",
        "interval": 15,
    }

}
